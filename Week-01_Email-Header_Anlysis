# Week 1 — Email Header Analysis 📨

## 🧠 Objective
Analyze full email headers to validate sender, trace origin, and detect spoofing or relay anomalies.

## 🧰 Tools Used
- MXToolbox (Header Analyzer)
- Google Admin Toolbox (Message header)
- VirusTotal / URLScan

## ⚙️ Steps Taken
1. Exported the **full headers** from the email client.
2. Parsed **Received** lines (top → bottom) to find the **originating IP**.
3. Compared **From**, **Return-Path**, **Sender**, and **Reply-To** for mismatches.
4. Checked **SPF**, **DKIM**, **DMARC** results (pass/fail/missing).
5. Ran IP/domain/URL reputation checks.
6. Documented anomalies and assessed risk.

## 🧩 Findings (Summary)
- Originating IP geolocates to **<region>** with **<reputation>**.
- **SPF:** fail | **DKIM:** missing | **DMARC:** none
- Evidence of **display name spoofing** or misaligned domains.
- URL redirects via **<shortener/redirect>** to **<domain>**.

## 📸 Screenshots
![Header Analyzer](./screenshots/header-analysis.png)
![IP Reputation](./screenshots/ip-reputation.png)

## 🧭 Lessons Learned
- Read **Received** chain carefully; bottom-most = earliest hop.
- Authentication failures are strong phishing indicators, not the only ones.
- Verify alignment across **From / Return-Path / DKIM**.
